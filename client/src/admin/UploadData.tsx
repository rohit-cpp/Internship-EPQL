import React from "react";

const UploadData = () => {
  return (
    <div>
      <div> Upload encrypted POI data to the system (via file or form)</div>
    </div>
  );
};

export default UploadData;
